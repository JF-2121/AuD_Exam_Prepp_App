---
id: sorting-basics
title: "Sorting Algorithms"
category: "Sorting"
order: 1
relatedAlgorithmIds: ["bubble-sort"]
---

## Overview

Sorting algorithms arrange elements of an array or list into a defined order (usually ascending).
This is placeholder sample content — it will be replaced with the real course material.

## Bubble Sort

Repeatedly steps through the array, comparing adjacent elements and swapping them if they are in the wrong order.

- Worst-case time: O(n²)
- Best-case time: O(n) (already sorted, with an early-exit optimization)
- Space: O(1) — sorts in place
- Stable: yes
